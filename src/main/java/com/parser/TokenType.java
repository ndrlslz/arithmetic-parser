package com.parser;

public enum TokenType {
    NUMBER,
    STRING,
    PUNCTUATION,
    KEY_WORD,
    VAR,
    OPERATION,
    ASSIGN,
    BINARY,
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    ID
}
